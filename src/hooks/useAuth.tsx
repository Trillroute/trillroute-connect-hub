
import { AuthProvider } from './useAuthProvider';
import { useProvideAuth } from './useProvideAuth';
import type { UserRole } from '@/types/auth';

export { AuthProvider, useProvideAuth as useAuth };
export type { UserRole };
